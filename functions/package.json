{
  "name": "wedplan-functions",
  "version": "1.0.0",
  "description": "Cloud Functions WedPlan: joinWedding (kolaborasi pasangan) + scheduledDeadlineReminder (push deadline).",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.3.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}